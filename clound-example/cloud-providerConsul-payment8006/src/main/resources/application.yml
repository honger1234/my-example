server:
  port: 8006

spring:
  application:
    name: cloud-payment-consul-service
  cloud:
    consul:
      host: localhost
      port: 8500
      discovery:
        service-name: ${spring.application.name}




